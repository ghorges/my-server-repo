

寄存器（内存访问）

1.
字型数据：就是两个字节那种的
任何两个地址连续的内存单元，N号单元和N+1号单元，可以看成
是两个内存单元，也可以看成一个地址为N的字单元中的高位字节
单元和低位字节单元。

2.DS和[address]
mov指令还可以将一个内存单元中的内容送入一个寄存器。
	mov al,[0]
执行指令时，8086cpu自动获取ds中的数据为内存单元的段地址

数据-》通用寄存器-》段寄存器

mov bx,1000
mov ds,bx
mov [0],al


4.mov，add，sub传送

mov ds,ax

双字：4个字节


5.数据段
DS


6.栈
PUSH
POP

这个栈说明，放进去之后就和ax，bx，cx没关系了，就可以随便改了

这个栈是内存地址大的是栈底，内存地址小的是栈顶。
SP开始就是在最下面。
偏移是push的时候每次-2
小端序，大大小小，一个字是2个字节，相当于一个short

1.cpu如何知道当前
SS存放栈顶的段地址
SP存放栈顶的偏移地址


~~~谈谈个人理解：为什么要这样实现，因为这样实现cpu才能知道你
		 这个栈是多少字节，每次存的时候，是先让SP-=2
		 然后再存，同理，每次取的时候是先取，后+2


8.栈顶越界问题
栈顶越界是危险的（黑客角度，溢出攻击！！！）

满的时候push，空的时候pop，都会发生那种问题













