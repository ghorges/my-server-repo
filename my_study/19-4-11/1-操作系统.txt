重要的点:
中断,异常和系统调用

cpu加电之后,执行的第一条指令在哪?

内存会分为:RAM随机访问存储,和ROM只读存储

内存中BIOS启动固件:
cs:ip设置制定的值

系统启动流程:
读BIOS,BIOS读加载程序,加载程序读内核映像.

BIOS读主引导扇区的代码(主引导记录)

主引导扇区代码读取活动分区的引导扇区代码.(活动分区)

读取文件系统的加载程序

CS:IP = 0xf000:fff0
读取第一条指令

BIOS硬件自检.
检测内存,显卡等关键部件是否存在.
BIOS的初始化.
按启动顺序读取第一块扇区.

主引导记录.
521字节,只能用446字节.
有启动代码.平台不同cpu启动代码不同.


加载程序的细化,读取启动配置文件,选择什么启动之类的(比如安全模式等其他模式).然后加载内核.



启动规范:BIOS有好多....
BIOS-MBR,BIOS-GPT,PXE(从网络启动)

UEFI
这个有签名什么的,安全,好用








中断,异常,和系统调用
